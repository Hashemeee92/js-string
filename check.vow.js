// Get a string to check vowels present or not
let str = "Aemisscountry";
// Conver to lower case
str=str.toLowerCase();
//check vowels whether present or not
if(
    str.includes("a") &&;
    str.includes("e") &&;
    str.includes("i") &&;
    str.includes("o") &&;
    str.includes("u")
)
{
    //if all vowels present the print 
    console.log("All vowels are present int string");
}
else{
    // all vowels are not present
    console.log("All vowels are not present in the string")
}